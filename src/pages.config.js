import Leads from './pages/Leads';
import __Layout from './Layout.jsx';


export const PAGES = {
    "Leads": Leads,
}

export const pagesConfig = {
    mainPage: "Leads",
    Pages: PAGES,
    Layout: __Layout,
};